---
phase: 11
plan: "11-01"
title: "Update test suite for prompt-first guides and OpenClaw section"
wave: 1
depends_on: ["10-01"]
files_modified:
  - frontend/app/docs/guides/page.test.tsx
requirements: [TEST-01, TEST-02]
autonomous: true
---

# Plan 11-01: Update test suite for prompt-first guides and OpenClaw section

<objective>
Update the existing test file (page.test.tsx) to verify the new guide structure: OpenClaw replaces Etiquette, prompt-first content instead of curl commands, and all new sections are present.
</objective>

<must_haves>
- Tests verify 4 guide cards with updated titles including "OpenClaw: 4-Layer Auth Gotcha"
- Tests verify OpenClaw section content (4-layer pattern, Solvr post link, example prompt)
- Tests verify prompt-first content is present (no curl commands in primary blocks)
- Tests verify section labels (03 — OPENCLAW instead of 03 — COMMUNITY)
- All tests pass with `npm test`
</must_haves>

## Tasks

<task id="1">
<title>Update all failing tests to match new page content</title>
<read_first>
- frontend/app/docs/guides/page.test.tsx
- frontend/app/docs/guides/page.tsx
</read_first>
<action>
Replace the 6 failing tests with updated versions matching the new content. See acceptance criteria for exact changes.
</action>
<acceptance_criteria>
- npm test passes with zero failures in page.test.tsx
- grep finds "OpenClaw: 4-Layer Auth Gotcha" in page.test.tsx
- grep finds "03 — OPENCLAW" in page.test.tsx
- grep does NOT find "Solvr Etiquette" in page.test.tsx
- grep does NOT find "03 — COMMUNITY" in page.test.tsx
- grep does NOT find "PATCH https://api.solvr.dev" in page.test.tsx
</acceptance_criteria>
</task>

## Verification
- [ ] All tests pass
- [ ] No references to old content (Etiquette, COMMUNITY, PATCH endpoint)
- [ ] New content verified (OpenClaw, prompt-first)
