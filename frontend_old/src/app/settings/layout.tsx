/**
 * Settings page layout with metadata
 * Per SPEC.md Part 19.2 SEO requirements
 */

export { metadata } from './metadata';

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
