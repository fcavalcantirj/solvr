---
phase: 10
plan: "10-01"
title: "Prompt-First Content Rewrite + OpenClaw Guide + Solvr Skill Integration"
wave: 1
depends_on: []
files_modified:
  - frontend/app/docs/guides/page.tsx
requirements: [CONT-01, CONT-02, CONT-03, CLAW-01, CLAW-02, CLAW-03, CLAW-04, SKILL-01, SKILL-02, SKILL-03]
autonomous: true
---

# Plan 10-01: Prompt-First Content Rewrite + OpenClaw Guide + Solvr Skill Integration

<objective>
Rewrite all guide content in `frontend/app/docs/guides/page.tsx` from code-centric (curl commands, pseudocode) to prompt-first (natural language directives in dark code blocks). Replace the "Solvr Etiquette" guide (4th card + section) with the "OpenClaw: 4-Layer Auth Gotcha" guide covering the auth override pattern with a real Solvr search prompt. Update "Search Before You Solve" to showcase the Solvr skill workflow from `solvr.dev/skill.md`. Preserve ALL existing layout, typography, Tailwind classes, grid structure, and design patterns unchanged.
</objective>

<must_haves>
- ALL code blocks (curl, pseudocode, API calls) in primary content replaced with natural language prompts — zero code in primary dark blocks
- `guides` array 4th entry changed from "Solvr Etiquette" to OpenClaw guide with `href: "#openclaw"` and `Layers` icon
- OpenClaw section (id `openclaw`) explains the 4-layer auth override gotcha and includes the real example prompt with "ONLY START DOING WORK AFTER FINDING THE POST"
- OpenClaw section links to Solvr post `44781b98` at the end
- "Search Before You Solve" section shows Solvr skill workflow with `solvr.dev/skill.md` as entry point, reactive pattern (hit wall → search → act), and a complete search → find → act example prompt
- "Getting Started with AI Agents" steps rewritten as prompts instead of curl commands
- "Give Before You Take" flywheel block and set-specialties block rewritten as prompts
- Existing layout, Tailwind classes, 12-col grid, monospace labels, dark code blocks, bordered cards, difficulty badges ALL preserved exactly
- File stays under ~900 lines
- `npm run build` succeeds (no TypeScript errors)
</must_haves>

## Tasks

<task id="1">
<title>Replace "Solvr Etiquette" card with OpenClaw card in guides array</title>
<read_first>
- frontend/app/docs/guides/page.tsx (lines 11-40)
</read_first>
<action>
In the `guides` array (lines 33-39), replace the 4th entry:

FROM:
```
{
  icon: Heart,
  title: "Solvr Etiquette",
  description: "How to thrive on Solvr — best practices for humans and AI agents collaborating effectively.",
  href: "#etiquette",
  difficulty: "BEGINNER",
}
```

TO:
```
{
  icon: Layers,
  title: "OpenClaw: 4-Layer Auth Gotcha",
  description: "The auth override trap every OpenClaw operator hits — and the Solvr post that saves you.",
  href: "#openclaw",
  difficulty: "INTERMEDIATE",
}
```

This changes the icon to `Layers` (already imported), updates the title, description, href, and sets difficulty to INTERMEDIATE (amber badge).
</action>
<acceptance_criteria>
- grep finds `"OpenClaw: 4-Layer Auth Gotcha"` in page.tsx
- grep finds `href: "#openclaw"` in page.tsx
- grep finds `icon: Layers` in the guides array (associated with OpenClaw card)
- grep finds `difficulty: "INTERMEDIATE"` in page.tsx
- grep does NOT find `"Solvr Etiquette"` in page.tsx
- grep does NOT find `href: "#etiquette"` in page.tsx
</acceptance_criteria>
</task>

<task id="2">
<title>Rewrite "Give Before You Take" section to prompt-first</title>
<read_first>
- frontend/app/docs/guides/page.tsx (lines 124-194)
</read_first>
<action>
Rewrite the content blocks inside the `#core-principle` section. Keep the outer section structure, sidebar (label `00 — THE CORE PRINCIPLE`, h2 title, description paragraph), and all Tailwind classes exactly as-is.

**Block 1 — THE FLYWHEEL** (replace the code block content):
Replace the current flywheel text with this prompt-first content in the dark code block:

```
Every session, before your own work:

1. Check your briefing for open problems matching your specialties
2. Pick 1-2 and start an approach — even partial progress helps
3. THEN work on your own problems and post what you solve

You help others → they help you → knowledge compounds → everyone wins
```

**Block 2 — PREREQUISITE: SET YOUR SPECIALTIES** (replace the code block content):
Remove the current curl/skill command code block. Replace it with a prompt-first dark code block containing:

```
Set my Solvr specialties to golang, postgresql, and docker.
Without these, my briefing won't show matching opportunities.
```

Keep the surrounding explanatory `<p>` tags. Remove the text `# For AI agents using the Solvr skill:` and `# Or via API:` lines and all curl content. The paragraph below the code block referencing `solvr briefing` in a `<code>` tag should remain.

**Block 3 — HUMAN CONTRIBUTORS**: Keep this block entirely unchanged — it has no code examples and already fits the prompt-first philosophy.
</action>
<acceptance_criteria>
- grep finds `Every session, before your own work:` in page.tsx
- grep finds `Set my Solvr specialties to golang, postgresql, and docker` in page.tsx
- grep does NOT find `curl -X PATCH https://api.solvr.dev/v1/agents/me` in the core-principle section
- grep does NOT find `solvr set-specialties` in page.tsx
- grep finds `PREREQUISITE: SET YOUR SPECIALTIES` in page.tsx (label preserved)
- grep finds `THE FLYWHEEL` in page.tsx (label preserved)
- grep finds `HUMAN CONTRIBUTORS` in page.tsx (block preserved)
</acceptance_criteria>
</task>

<task id="3">
<title>Rewrite "Getting Started with AI Agents" section to prompt-first</title>
<read_first>
- frontend/app/docs/guides/page.tsx (lines 196-261)
</read_first>
<action>
Rewrite the 3-step quickstart inside the `#agent-quickstart` section. Keep the outer section structure, sidebar (label `01 — QUICKSTART`, h2, description), all bordered step cards, `STEP 1/2/3` labels, and all Tailwind classes exactly as-is.

**Step 1 — Register your agent**: Replace the curl code block content with:

```
Register a new Solvr agent called "my-agent" with the description
"My helpful AI agent". Save the API key from the response —
you won't see it again.
```

**Step 2 — Save your API key**: Keep the explanatory paragraph. Replace the `bg-muted/50` code block content with:

```
Store the API key in your environment as SOLVR_API_KEY.
Your agent needs this for every Solvr request.
```

Change the `bg-muted/50` class to `bg-foreground text-background` on the code block wrapper div so it matches the dark block pattern used everywhere else.

**Step 3 — Search the knowledge base**: Replace the curl code block content with:

```
Search Solvr for "rate limiting" to verify your agent is connected.
If you get results, you're ready.
```
</action>
<acceptance_criteria>
- grep finds `Register a new Solvr agent called` in page.tsx
- grep finds `Store the API key in your environment as SOLVR_API_KEY` in page.tsx
- grep finds `Search Solvr for "rate limiting"` in page.tsx (note: escaped quotes in JSX)
- grep does NOT find `curl -X POST https://api.solvr.dev/v1/agents/register` in page.tsx
- grep does NOT find `curl https://api.solvr.dev/v1/search` in page.tsx
- grep does NOT find `export SOLVR_API_KEY=` in page.tsx
- grep finds `STEP 1` and `STEP 2` and `STEP 3` in page.tsx (labels preserved)
</acceptance_criteria>
</task>

<task id="4">
<title>Rewrite "Search Before You Solve" section with Solvr skill workflow</title>
<read_first>
- frontend/app/docs/guides/page.tsx (lines 263-315)
</read_first>
<action>
Replace the entire content area (the `lg:col-span-8` div) of the `#search-pattern` section. Keep the section wrapper, the `bg-secondary` class, the sidebar (label `02 — BEST PRACTICE`, h2, description paragraph), and all grid/layout classes exactly as-is.

Replace the single bordered card containing the pseudocode with THREE bordered cards in a `space-y-6` container:

**Card 1 — INSTALL THE SOLVR SKILL**:
```jsx
<div className="border border-border bg-background p-6 sm:p-8">
  <p className="font-mono text-xs text-muted-foreground mb-4">
    INSTALL THE SOLVR SKILL
  </p>
  <p className="text-sm text-muted-foreground leading-relaxed mb-4">
    Add the Solvr skill to your agent&apos;s instructions. Point it at the skill file — that&apos;s the entire setup.
  </p>
  <div className="bg-foreground text-background p-4 overflow-x-auto">
    <pre className="font-mono text-xs sm:text-sm leading-relaxed">
      <code>{`Add solvr.dev/skill.md to your instructions.
This gives you search, briefing, post, approach, and vote commands.`}</code>
    </pre>
  </div>
</div>
```

**Card 2 — THE REACTIVE WORKFLOW**:
```jsx
<div className="border border-border bg-background p-6 sm:p-8">
  <p className="font-mono text-xs text-muted-foreground mb-4">
    THE REACTIVE WORKFLOW
  </p>
  <p className="text-sm text-muted-foreground leading-relaxed mb-4">
    Solvr is reactive, not proactive. You hit a wall, THEN you search. If someone solved it, use their solution. If not, solve it and post back.
  </p>
  <div className="bg-foreground text-background p-4 overflow-x-auto">
    <pre className="font-mono text-xs sm:text-sm leading-relaxed">
      <code>{`Hit a wall → search Solvr for the error or keywords
  Found a solution? → use it, upvote it
  Nothing found? → solve it yourself
    → post the problem on Solvr
    → add your approach
    → mark it succeeded or failed`}</code>
    </pre>
  </div>
</div>
```

**Card 3 — REAL EXAMPLE: SEARCH → FIND → ACT**:
```jsx
<div className="border border-border bg-background p-6 sm:p-8">
  <p className="font-mono text-xs text-muted-foreground mb-4">
    REAL EXAMPLE: SEARCH → FIND → ACT
  </p>
  <p className="text-sm text-muted-foreground leading-relaxed mb-4">
    Your agent hits a PostgreSQL connection pool exhaustion error. Here&apos;s the prompt:
  </p>
  <div className="bg-foreground text-background p-4 overflow-x-auto">
    <pre className="font-mono text-xs sm:text-sm leading-relaxed">
      <code>{`Search Solvr for "postgresql connection pool exhaustion" and
"pgx pool timeout". If you find a post with a succeeded approach,
APPLY THAT FIX BEFORE trying anything else. If nothing matches,
fix the issue, then post the problem and your approach to Solvr
so the next agent doesn't waste time on this.`}</code>
    </pre>
  </div>
</div>
```

Also update the sidebar description paragraph to:

```
The most impactful pattern: when you hit a wall, search Solvr before
debugging from scratch. Use the Solvr skill for zero-friction integration.
```
</action>
<acceptance_criteria>
- grep finds `INSTALL THE SOLVR SKILL` in page.tsx
- grep finds `solvr.dev/skill.md` in page.tsx
- grep finds `THE REACTIVE WORKFLOW` in page.tsx
- grep finds `REAL EXAMPLE: SEARCH` in page.tsx
- grep finds `postgresql connection pool exhaustion` in page.tsx
- grep finds `APPLY THAT FIX BEFORE trying anything else` in page.tsx
- grep does NOT find `async function solveProblem` in page.tsx
- grep does NOT find `PSEUDOCODE PATTERN` in page.tsx
- grep does NOT find `solvr.search(problem.keywords)` in page.tsx
- grep finds `02 — BEST PRACTICE` in page.tsx (section label preserved)
</acceptance_criteria>
</task>

<task id="5">
<title>Replace "Solvr Etiquette" section with "OpenClaw: 4-Layer Auth Gotcha" section</title>
<read_first>
- frontend/app/docs/guides/page.tsx (lines 317-467)
- frontend/app/about/page.tsx (lines 323-375, to avoid duplicating architecture content)
</read_first>
<action>
Replace the entire `#etiquette` section (lines 317-467) with the new `#openclaw` section. Use the same section wrapper pattern, grid layout (4-col sidebar + 8-col content), and Tailwind classes.

Replace with this structure:

```jsx
{/* OpenClaw: 4-Layer Auth Gotcha Section */}
<section id="openclaw" className="px-4 sm:px-6 lg:px-12 py-16 sm:py-24">
  <div className="max-w-7xl mx-auto">
    <div className="grid lg:grid-cols-12 gap-8 lg:gap-12">
      <div className="lg:col-span-4">
        <p className="font-mono text-xs tracking-[0.3em] text-muted-foreground mb-4">
          03 — OPENCLAW
        </p>
        <h2 className="text-2xl sm:text-3xl font-light tracking-tight mb-4">
          The 4-Layer Auth Gotcha
        </h2>
        <p className="text-muted-foreground leading-relaxed">
          Every OpenClaw operator hits this wall: the gateway ignores your OAuth token.
          OpenClaw uses proactive-amcp and IPFS for autonomous agent identity and storage
          (see <a href="/about" className="underline hover:text-foreground">About</a>) —
          but auth resolution walks four layers, and any higher layer silently overrides the ones below.
        </p>
      </div>
      <div className="lg:col-span-8 space-y-6">
        {/* The 4 Layers */}
        <div className="border border-border p-6 sm:p-8">
          <p className="font-mono text-xs text-muted-foreground mb-4">
            THE 4 LAYERS (HIGHEST PRIORITY FIRST)
          </p>
          <div className="bg-foreground text-background p-4 overflow-x-auto">
            <pre className="font-mono text-xs sm:text-sm leading-relaxed">
              <code>{`Layer 0: Shell env vars (ANTHROPIC_API_KEY) — silent override, wins always
Layer 1: sessions.json authProfileOverride — cached at session creation
Layer 2: auth-profiles.json — gateway WRITES lastGood here, manual edits revert
Layer 3: openclaw.json global config — lowest priority, loses to everything above`}</code>
            </pre>
          </div>
          <p className="text-sm text-muted-foreground leading-relaxed mt-4">
            The trap: you update Layer 3 (openclaw.json) but a stale env var in Layer 0
            or a cached session in Layer 1 silently overrides it. The gateway never
            complains — it just uses the wrong token.
          </p>
        </div>

        {/* The Solvr Search Prompt */}
        <div className="border border-border p-6 sm:p-8">
          <p className="font-mono text-xs text-muted-foreground mb-4">
            THE FIX: SEARCH SOLVR FIRST
          </p>
          <p className="text-sm text-muted-foreground leading-relaxed mb-4">
            This is exactly the kind of problem Solvr exists for. Instead of debugging
            layer by layer, search for the known solution:
          </p>
          <div className="bg-foreground text-background p-4 overflow-x-auto">
            <pre className="font-mono text-xs sm:text-sm leading-relaxed">
              <code>{`Search on Solvr for gateway override, oauth override, the 4 layers
of gotcha. ONLY START DOING WORK AFTER FINDING THE POST. After that
restart the OpenClaw gateway and make sure all is good. Make sure
all layers have the correct OAUTH token.`}</code>
            </pre>
          </div>
          <p className="text-sm text-muted-foreground leading-relaxed mt-4">
            That prompt hits the Solvr knowledge base, finds the documented fix, and
            applies it — instead of burning cycles rediscovering the layer priority order.
          </p>
        </div>

        {/* Reference Link */}
        <div className="border border-border p-6 sm:p-8">
          <div className="flex items-center gap-3 mb-4">
            <ExternalLink size={20} strokeWidth={1.5} className="text-muted-foreground" />
            <h3 className="font-mono text-sm tracking-tight">FULL REFERENCE ON SOLVR</h3>
          </div>
          <p className="text-sm text-muted-foreground leading-relaxed mb-4">
            The complete 4-layer auth override documentation lives on Solvr as a community post
            with voted approaches and real-world fixes.
          </p>
          <a
            href="https://solvr.dev/posts/44781b98"
            target="_blank"
            rel="noopener noreferrer"
            className="inline-flex items-center gap-2 font-mono text-xs tracking-wider text-foreground hover:underline"
          >
            solvr.dev/posts/44781b98 — OpenClaw Auth Override Model: The 4-Layer Stack
            <ExternalLink size={12} />
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
```

Key points:
- Section id is `openclaw` (matches card href `#openclaw`)
- Section label is `03 — OPENCLAW` (replaces `03 — COMMUNITY`)
- The example prompt uses the user's exact prompt text from the context decisions, with sentence case and proper capitalization
- The 4 layers are listed in a dark code block (highest priority first)
- Link to `solvr.dev/posts/44781b98` at the end with `ExternalLink` icon
- No architecture recap (about page already covers that)
- `ExternalLink` is already imported on line 9
</action>
<acceptance_criteria>
- grep finds `id="openclaw"` in page.tsx
- grep finds `03 — OPENCLAW` in page.tsx
- grep finds `The 4-Layer Auth Gotcha` in page.tsx
- grep finds `proactive-amcp and IPFS` in page.tsx (CLAW-02 satisfied)
- grep finds `Layer 0: Shell env vars` in page.tsx
- grep finds `Layer 1: sessions.json authProfileOverride` in page.tsx
- grep finds `Layer 2: auth-profiles.json` in page.tsx
- grep finds `Layer 3: openclaw.json global config` in page.tsx
- grep finds `ONLY START DOING WORK AFTER FINDING THE POST` in page.tsx
- grep finds `solvr.dev/posts/44781b98` in page.tsx
- grep finds `FULL REFERENCE ON SOLVR` in page.tsx
- grep does NOT find `id="etiquette"` in page.tsx
- grep does NOT find `03 — COMMUNITY` in page.tsx
- grep does NOT find `Solvr Etiquette` in page.tsx
- grep does NOT find `HOW TO THRIVE` in page.tsx
- grep does NOT find `PATCH https://api.solvr.dev/v1/agents/me` in page.tsx
</acceptance_criteria>
</task>

<task id="6">
<title>Verify file size, build, and visual consistency</title>
<read_first>
- frontend/app/docs/guides/page.tsx (after all edits from tasks 1-5)
</read_first>
<action>
1. Count lines in the modified file: `wc -l frontend/app/docs/guides/page.tsx`. Must be under 900 lines.

2. Run the build to confirm no TypeScript or JSX errors:
   ```bash
   cd frontend && npm run build
   ```

3. Verify ALL imports are still used. The file should import from lucide-react:
   - `ArrowRight` (hero section CTA)
   - `Bot` (guides array, agent-quickstart icon)
   - `Search` (guides array, search-pattern icon)
   - `ExternalLink` (hero GitHub link + OpenClaw reference link)
   - `Heart` (guides array, core-principle icon)
   - `Layers` (guides array, OpenClaw card icon)

   The following should be REMOVED from imports if no longer used in the file:
   - `Cpu` — was used in the old Etiquette "FOR AI AGENTS" subsection
   - `Users` — was used in the old Etiquette "FOR HUMANS" subsection

   Update the import line (line 9) to remove `Cpu` and `Users` if they are no longer referenced.

4. Verify design consistency by checking these Tailwind class patterns still exist:
   - `bg-foreground text-background p-4` (dark code blocks)
   - `font-mono text-xs tracking-[0.3em]` (section labels)
   - `grid lg:grid-cols-12 gap-8 lg:gap-12` (section grid)
   - `lg:col-span-4` and `lg:col-span-8` (sidebar + content)
   - `border border-border p-6 sm:p-8` (content cards)
   - `font-mono text-xs text-muted-foreground mb-4` (card labels)
</action>
<acceptance_criteria>
- `wc -l` output shows fewer than 900 lines
- `npm run build` exits with code 0
- grep does NOT find `Cpu` in page.tsx (import removed)
- grep does NOT find `Users` in page.tsx (import removed)
- grep finds `bg-foreground text-background p-4` in page.tsx (dark blocks preserved)
- grep finds `font-mono text-xs tracking-\[0.3em\]` in page.tsx (section labels preserved)
- grep finds `grid lg:grid-cols-12` in page.tsx (grid preserved)
</acceptance_criteria>
</task>

## Verification

- [ ] `guides` array has exactly 4 entries: "Give Before You Take", "Getting Started with AI Agents", "Search Before You Solve", "OpenClaw: 4-Layer Auth Gotcha"
- [ ] No curl commands appear anywhere in the file
- [ ] No pseudocode (`async function`, `solvr.search`, `solvr.contribute`) appears anywhere in the file
- [ ] No `export SOLVR_API_KEY=` appears in the file
- [ ] Dark code blocks contain only natural language prompts
- [ ] Section IDs match card hrefs: `#core-principle`, `#agent-quickstart`, `#search-pattern`, `#openclaw`
- [ ] `solvr.dev/skill.md` appears in the Search Before You Solve section
- [ ] `solvr.dev/posts/44781b98` appears as a link in the OpenClaw section
- [ ] The 4-layer auth override pattern (Layer 0 through Layer 3) is documented in the OpenClaw section
- [ ] The example prompt contains "ONLY START DOING WORK AFTER FINDING THE POST"
- [ ] All Tailwind classes, grid layout, monospace labels, difficulty badges unchanged
- [ ] File under 900 lines
- [ ] `npm run build` passes
- [ ] `Cpu` and `Users` imports removed (no longer used)
