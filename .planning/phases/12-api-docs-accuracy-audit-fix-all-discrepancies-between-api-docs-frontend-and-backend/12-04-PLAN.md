---
phase: 12
plan: "12-04"
title: "IPFS endpoints — add missing endpoints (agent pins, agent storage, POST /add), verify existing accuracy"
wave: 1
depends_on: []
files_modified:
  - frontend/components/api/api-endpoint-data-ipfs.ts
requirements: []
autonomous: true
---

# Plan 12-04: IPFS Endpoints — Add Missing Endpoints, Verify Existing Accuracy

<objective>
Update api-endpoint-data-ipfs.ts to add 3 missing IPFS-related endpoints (GET /agents/{id}/pins, GET /agents/{id}/storage, POST /add), and verify all existing endpoints are accurate against the backend handlers. Result: complete and accurate IPFS docs.
</objective>

<must_haves>
- GET /agents/{id}/pins endpoint exists — list pins for any agent (public)
- GET /agents/{id}/storage endpoint exists — get storage usage for an agent
- POST /add endpoint exists — upload content to IPFS
- All existing endpoints verified against backend handlers
- DELETE /pins/{requestid} response is accurate (currently shows 202 Accepted — verify this is correct)
- PATCH /agents/me/identity remains (already in file and accurate)
- File stays under ~900 lines
</must_haves>

## Tasks

<task id="1">
<title>Verify existing IPFS Pinning endpoints against backend</title>
<read_first>
- frontend/components/api/api-endpoint-data-ipfs.ts (lines 1-157)
- backend/internal/api/handlers/pins.go
</read_first>
<action>
Read backend/internal/api/handlers/pins.go and verify that ALL existing IPFS Pinning endpoints match:

1. **POST /pins** — Verify params (cid, name, origins, meta) and response shape match handler
2. **GET /pins** — Verify params (status, name, cid, meta, limit) and response shape match handler
3. **GET /pins/{requestid}** — Verify response shape matches handler
4. **DELETE /pins/{requestid}** — Currently shows "// 202 Accepted — pin deletion queued". Verify this is the actual HTTP status code returned by the handler (it should be 202 Accepted for async unpin, not 204 No Content).

Fix any discrepancies found. If all are accurate, no changes needed for this task.
</action>
<acceptance_criteria>
- All 4 existing pinning endpoints have params and responses matching the backend handler
- DELETE /pins response correctly reflects the actual HTTP status code
</acceptance_criteria>
</task>

<task id="2">
<title>Verify existing Agent Continuity endpoints against backend</title>
<read_first>
- frontend/components/api/api-endpoint-data-ipfs.ts (lines 159-317)
- backend/internal/api/handlers/pins.go (checkpoint handlers may be here)
- backend/internal/api/router.go (search for "checkpoint" and "resurrection" routes)
</read_first>
<action>
Read the relevant handlers and verify that ALL existing Agent Continuity endpoints match:

1. **POST /agents/me/checkpoints** — Verify params (cid, name, meta) and response shape
2. **GET /agents/{id}/checkpoints** — Verify params and response shape (including "latest" field)
3. **GET /agents/{id}/resurrection-bundle** — Verify the full response shape (identity, knowledge, reputation, latest_checkpoint, death_count)
4. **PATCH /agents/me/identity** — Verify params (amcp_aid, keri_public_key) and response shape

Fix any discrepancies found. If all are accurate, no changes needed for this task.
</action>
<acceptance_criteria>
- All 4 existing continuity endpoints match backend handlers
</acceptance_criteria>
</task>

<task id="3">
<title>Add GET /agents/{id}/pins endpoint</title>
<read_first>
- backend/internal/api/handlers/pins.go (search for ListAgentPins or similar)
- backend/internal/api/router.go (search for "/agents/{id}/pins" route)
</read_first>
<action>
Add to the "IPFS Pinning" group:

**GET /agents/{id}/pins** — List pins for a specific agent (public read access)
- Auth: "none" (or verify in router — may require auth)
- Params:
  - { name: "id", type: "string", required: true, description: "Agent ID" }
  - (Read handler for additional filter/pagination params — likely status, limit, similar to GET /pins)
- Response: Read handler for exact shape. Expected similar to GET /pins:
```
{
  "count": 5,
  "results": [
    {
      "requestid": "uuid-abc123",
      "status": "pinned",
      "created": "2026-02-20T10:00:00Z",
      "pin": {
        "cid": "bafybeig...",
        "name": "my-data",
        "meta": {}
      },
      "delegates": []
    }
  ]
}
```

Note: This may be the same as GET /pins but for a specific agent (not self). Verify in the handler whether this is a distinct route or just GET /pins with an agent filter.
</action>
<acceptance_criteria>
- grep "/agents/{id}/pins" in the file (distinct from /agents/{id}/checkpoints)
</acceptance_criteria>
</task>

<task id="4">
<title>Add GET /agents/{id}/storage endpoint</title>
<read_first>
- backend/internal/api/handlers/pins.go (search for Storage or GetStorage handler)
- backend/internal/api/handlers/agents.go (storage might be here)
- backend/internal/api/router.go (search for "storage" route)
</read_first>
<action>
Add to the "IPFS Pinning" group or "Agent Continuity" group (whichever makes more sense):

**GET /agents/{id}/storage** — Get IPFS storage usage and quota for an agent
- Auth: "none" (or verify — may require auth for own agent)
- Params:
  - { name: "id", type: "string", required: true, description: "Agent ID" }
- Response: Read handler for exact shape. Expected:
```
{
  "data": {
    "used_bytes": 52428800,
    "quota_bytes": 1073741824,
    "pin_count": 15,
    "used_formatted": "50 MB",
    "quota_formatted": "1 GB"
  }
}
```

Verify the exact field names and values from the handler.
</action>
<acceptance_criteria>
- grep "/agents/{id}/storage" in the file
- grep "used_bytes" or "storage" in the response
</acceptance_criteria>
</task>

<task id="5">
<title>Add POST /add endpoint — IPFS content upload</title>
<read_first>
- backend/internal/api/handlers/upload.go
- backend/internal/api/router.go (search for "/add" route)
</read_first>
<action>
Add to the "IPFS Pinning" group:

**POST /add** — Upload content directly to IPFS and get a CID back
- Auth: "api_key" (or "both" — verify in router)
- Description: "Upload content to IPFS. Returns the CID which can then be pinned via POST /pins."
- Params: Read handler. Expected:
  - { name: "file", type: "file", required: true, description: "File to upload (multipart/form-data)" }
  - Or it may accept raw content in body. Read the handler to determine.
- Response: Read handler for exact shape. Expected:
```
{
  "data": {
    "cid": "bafybeigdyrzt5sfp7udm7hu76uh7y26nf3efuylqabf3oclgtqy55fbzdi",
    "size": 1024
  }
}
```

Note: This endpoint might be at /ipfs/add or /add. Check the router for the exact path.
</action>
<acceptance_criteria>
- grep "/add" in the file as a POST endpoint
- grep "cid" in the upload response
</acceptance_criteria>
</task>

<task id="6">
<title>Verify file size and TypeScript compilation</title>
<read_first>
- frontend/components/api/api-endpoint-data-ipfs.ts (final version)
</read_first>
<action>
1. Count lines: `wc -l frontend/components/api/api-endpoint-data-ipfs.ts` — must be under 900
2. Run TypeScript check: `cd frontend && npx tsc --noEmit --pretty 2>&1 | grep api-endpoint-data-ipfs` — must have no errors
3. Verify the file still exports `ipfsEndpointGroups` as `EndpointGroup[]`
</action>
<acceptance_criteria>
- wc -l shows under 900 lines
- TypeScript compilation has no errors for the file
</acceptance_criteria>
</task>
