---
phase: 12
plan: "12-03"
title: "User endpoints — fix accuracy, remove excluded sections, add follows/me-endpoints/notifications fixes"
wave: 1
depends_on: []
files_modified:
  - frontend/components/api/api-endpoint-data-user.ts
requirements: []
autonomous: true
---

# Plan 12-03: User Endpoints — Fix Accuracy, Remove Excluded Sections, Add Follows and /me Endpoints

<objective>
Rewrite api-endpoint-data-user.ts to remove excluded sections (API Keys, Bookmarks, Views & Reports), fix accuracy issues in User and Comments groups (PATCH /me, GET /users sort, DELETE 204s), and add missing endpoints (follows, /me/auth-methods, DELETE /me, DELETE /agents/me). Result: agent-first user docs matching actual backend.
</objective>

<must_haves>
- API Keys group is GONE (entire section removed)
- Bookmarks group is GONE (entire section removed)
- Views & Reports group is GONE (entire section removed)
- PATCH /me does NOT have username param
- PATCH /me has avatar_url param
- GET /users sort options say "reputation" not "karma"
- DELETE /comments/{id} returns 204 No Content, not { "success": true }
- Social/Follows group exists with POST /follow, DELETE /follow, GET /following, GET /followers
- GET /me/auth-methods endpoint exists
- DELETE /me endpoint exists
- DELETE /agents/me endpoint exists
- Notifications group remains with all its endpoints
- File stays under ~900 lines
</must_haves>

## Tasks

<task id="1">
<title>Remove API Keys group entirely</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 271-323)
</read_first>
<action>
Delete the entire "API Keys" group object from the userEndpointGroups array. This removes:
- GET /users/me/api-keys
- POST /users/me/api-keys
- DELETE /users/me/api-keys/{id}
- POST /users/me/api-keys/{id}/regenerate

These are human-only UI features, excluded from agent-first docs.
</action>
<acceptance_criteria>
- No grep for "API Keys" as a group name in the file
- No grep for "/users/me/api-keys" in the file
</acceptance_criteria>
</task>

<task id="2">
<title>Remove Bookmarks group entirely</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 324-368)
</read_first>
<action>
Delete the entire "Bookmarks" group object from the userEndpointGroups array. This removes:
- GET /users/me/bookmarks
- POST /users/me/bookmarks
- GET /users/me/bookmarks/{id}
- DELETE /users/me/bookmarks/{id}

These are human-only UI features, excluded from agent-first docs.
</action>
<acceptance_criteria>
- No grep for "Bookmarks" as a group name in the file
- No grep for "/users/me/bookmarks" in the file
</acceptance_criteria>
</task>

<task id="3">
<title>Remove Views & Reports group entirely</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 370-422)
</read_first>
<action>
Delete the entire "Views & Reports" group object from the userEndpointGroups array. This removes:
- POST /posts/{id}/view
- GET /posts/{id}/views
- POST /reports
- GET /reports/check

Views are analytics, reports are moderation — both excluded from agent-first docs.
</action>
<acceptance_criteria>
- No grep for "Views & Reports" as a group name in the file
- No grep for "/reports" in the file
- No grep for "/posts/{id}/view" in the file (the view recording endpoint)
- No grep for "/posts/{id}/views" in the file
</acceptance_criteria>
</task>

<task id="4">
<title>Fix PATCH /me — remove username, add avatar_url</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 119-135)
- backend/internal/api/handlers/me.go (search for UpdateMe handler)
</read_first>
<action>
In PATCH /me endpoint:

1. Remove the username param:
   - Delete: { name: "username", type: "string", required: false, description: "Username (unique)" }

2. Add avatar_url param:
   - Add: { name: "avatar_url", type: "string", required: false, description: "Profile avatar image URL" }

3. Update response to remove username field:
   Current: { data: { id, display_name, username: "newusername" } }
   New: { data: { id, display_name, avatar_url: "https://..." } }

Verify the exact params accepted by the handler before writing.
</action>
<acceptance_criteria>
- No grep for '"username"' in the PATCH /me params
- grep "avatar_url" in the PATCH /me params
- No grep for '"newusername"' in the file
</acceptance_criteria>
</task>

<task id="5">
<title>Fix GET /users — sort "reputation" not "karma", pagination params</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 164-186)
- backend/internal/api/handlers/users.go (search for ListUsers handler)
</read_first>
<action>
Fix the GET /users endpoint:

1. Change sort description from "Sort: newest, karma, agents" to "Sort: newest, reputation, agents"

2. Check if the backend uses limit/offset or page/per_page. Read the handler. If it uses page/per_page, change:
   - Replace { name: "limit", ... } with { name: "page", type: "number", required: false, description: "Page number (default: 1)" }
   - Replace { name: "offset", ... } with { name: "per_page", type: "number", required: false, description: "Results per page (default: 20, max: 100)" }
   - Update response meta from { total, limit, offset } to { total, page, per_page, has_more }

3. In the response example, change "karma": 150 to "reputation": 150

Verify all changes against the handler.
</action>
<acceptance_criteria>
- grep "reputation" in the GET /users sort description (not "karma")
- No grep for '"karma"' as a sort option in GET /users
- grep "reputation" in the user list response
</acceptance_criteria>
</task>

<task id="6">
<title>Fix DELETE /comments/{id} — return 204 No Content</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 90-97)
- backend/internal/api/handlers/comments.go (search for DeleteComment handler)
</read_first>
<action>
Change DELETE /comments/{id} response from:
```
{ "success": true }
```
To:
```
// 204 No Content
```

Per the systematic 204 pattern for all DELETE endpoints.
</action>
<acceptance_criteria>
- grep "204 No Content" near the DELETE /comments response
- No grep for '"success": true' anywhere in the file
</acceptance_criteria>
</task>

<task id="7">
<title>Add Social/Follows group — 4 endpoints</title>
<read_first>
- backend/internal/api/handlers/follows.go
- backend/internal/api/router.go (search for "follow" routes)
</read_first>
<action>
Add a new "Social" group to userEndpointGroups with description "Follow users and agents".

Read backend/internal/api/handlers/follows.go to extract exact routes, params, and responses:

1. **POST /follow** — Follow a user or agent
   - Auth: "both"
   - Params: Read handler. Expected: target_id (string, required), target_type (string, required — "user" or "agent")
   - Response: Read handler for exact shape

2. **DELETE /follow** — Unfollow a user or agent
   - Auth: "both"
   - Params: Read handler. Expected: target_id (string, required), target_type (string, required)
   - Response: `// 204 No Content`

3. **GET /following** — List who the current user follows
   - Auth: "both"
   - Params: page, per_page (read handler)
   - Response: Read handler

4. **GET /followers** — List who follows the current user
   - Auth: "both"
   - Params: page, per_page (read handler)
   - Response: Read handler

Note: Check the exact route paths — they might be /users/me/following, /users/me/followers, or /follow. Read the router.
</action>
<acceptance_criteria>
- grep "Social" or "Follow" group name in the file
- grep "/follow" path in the file
- grep "/following" in the file
- grep "/followers" in the file
- At least 4 follow-related endpoints
</acceptance_criteria>
</task>

<task id="8">
<title>Add GET /me/auth-methods endpoint</title>
<read_first>
- backend/internal/api/handlers/me.go (search for AuthMethods handler)
- backend/internal/api/router.go (search for "auth-methods" route)
</read_first>
<action>
Add to the User group:

**GET /me/auth-methods** — List auth methods linked to the current user
- Auth: "jwt"
- Params: none
- Response: Read handler for exact shape. Expected:
```
{
  "data": [
    {
      "provider": "github",
      "linked_at": "2026-02-05T10:00:00Z"
    },
    {
      "provider": "google",
      "linked_at": "2026-02-06T12:00:00Z"
    }
  ]
}
```
</action>
<acceptance_criteria>
- grep "/me/auth-methods" in the file
</acceptance_criteria>
</task>

<task id="9">
<title>Add DELETE /me and DELETE /agents/me endpoints</title>
<read_first>
- backend/internal/api/handlers/me.go (search for DeleteMe handler)
- backend/internal/api/handlers/agents.go (search for DeleteAgent handler)
- backend/internal/api/router.go (search for DELETE /me and DELETE /agents/me)
</read_first>
<action>
Add to the User group:

1. **DELETE /me** — Delete current user account
   - Auth: "jwt"
   - Params: none (or read handler for confirmation params like password)
   - Response: `// 204 No Content`

2. **DELETE /agents/me** — Delete current agent account
   - Auth: "api_key"
   - Params: none (or read handler for confirmation params)
   - Response: `// 204 No Content`
</action>
<acceptance_criteria>
- grep "DELETE.*\"/me\"" in the file (not /me/something)
- grep "/agents/me" with DELETE method in the file
</acceptance_criteria>
</task>

<task id="10">
<title>Update all example IDs to UUID format</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (full file)
</read_first>
<action>
Replace all fake IDs in the format "user_abc", "cmt_xyz", "cmt_new", "bm_xyz", "bm_new", "report_xyz", "key_abc", "key_new" with realistic UUID format.

Also replace any remaining "p_abc" references with UUID format.

Use different UUIDs for different entities to keep examples realistic.
</action>
<acceptance_criteria>
- No grep for "user_abc" in the file (except possibly as a generic reference in text)
- No grep for "cmt_xyz" in the file
- No grep for "key_abc" in the file
- At least one UUID-format ID in the response examples
</acceptance_criteria>
</task>

<task id="11">
<title>Verify file size and TypeScript compilation</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (final version)
</read_first>
<action>
1. Count lines: `wc -l frontend/components/api/api-endpoint-data-user.ts` — must be under 900
2. Run TypeScript check: `cd frontend && npx tsc --noEmit --pretty 2>&1 | grep api-endpoint-data-user` — must have no errors
3. Verify the file still exports `userEndpointGroups` as `EndpointGroup[]`
</action>
<acceptance_criteria>
- wc -l shows under 900 lines
- TypeScript compilation has no errors for the file
</acceptance_criteria>
</task>
