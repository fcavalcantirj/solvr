---
phase: 12
plan: "12-03"
title: "Fix api-endpoint-data-user.ts"
wave: 1
depends_on: []
files_modified:
  - frontend/components/api/api-endpoint-data-user.ts
requirements: []
autonomous: true
---

# Plan 12-03: Fix api-endpoint-data-user.ts

<objective>
Fix all discrepancies in api-endpoint-data-user.ts (Comments, User, API Keys, Bookmarks, Views & Reports groups) so the /api-docs page matches the actual backend handlers. Fix wrong params, wrong responses, add missing endpoints.
</objective>

<must_haves>
- PATCH /me: username param removed, avatar_url param added
- GET /users: sort option "karma" changed to "reputation", meta includes has_more and total_backed_agents
- DELETE /comments/{id} returns 204 No Content (not JSON)
- DELETE /users/me/api-keys/{id} returns 204 No Content (not JSON)
- DELETE /users/me/bookmarks/{id} returns 204 No Content (not JSON)
- POST /users/me/api-keys/{id}/regenerate response includes name, user_id, created_at, updated_at
- GET /me/auth-methods endpoint is documented
- DELETE /me endpoint is documented
- GET /users/{id}/badges endpoint is documented
- Follows (4 endpoints) are documented
- GET /heartbeat endpoint is documented
- File stays under ~900 lines
</must_haves>

## Tasks

<task id="1">
<title>Fix PATCH /me: remove username, add avatar_url</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 118-135)
- backend/internal/api/handlers/users.go (lines 92-97 UpdateProfileRequest, lines 322-399 UpdateProfile)
</read_first>
<action>
1. Remove `username` param from PATCH /me (backend UpdateProfileRequest does NOT accept username)
2. Add `avatar_url` param: `{ name: "avatar_url", type: "string", required: false, description: "Avatar URL" }`
3. Update response example — remove username field, ensure it shows display_name and avatar_url
</action>
<acceptance_criteria>
- grep "username" does NOT appear in PATCH /me params
- grep "avatar_url" appears in PATCH /me params
</acceptance_criteria>
</task>

<task id="2">
<title>Fix GET /users: karma to reputation, add meta fields</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 165-186)
- backend/internal/api/handlers/users.go (lines 240-310 ListUsers, lines 161-173 UsersListResponse)
</read_first>
<action>
1. Fix sort param: change "newest, karma, agents" to "newest, reputation, agents"
2. Fix response data: change `"karma": 150` to `"reputation": 150`
3. Fix response meta to match backend UsersListResponse.Meta:
```json
"meta": { "total": 100, "limit": 20, "offset": 0, "has_more": true, "total_backed_agents": 45 }
```
</action>
<acceptance_criteria>
- grep "reputation" appears in GET /users sort description (not "karma")
- grep "total_backed_agents" appears in GET /users meta response
- grep "has_more" appears in GET /users meta response
</acceptance_criteria>
</task>

<task id="3">
<title>Fix DELETE /comments/{id}: 204 No Content</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 90-97)
- backend/internal/api/handlers/comments.go (line 251: w.WriteHeader(http.StatusNoContent))
</read_first>
<action>
Change DELETE /comments/{id} response from:
```
`{ "success": true }`
```
to:
```
`// 204 No Content`
```
</action>
<acceptance_criteria>
- grep "204 No Content" appears for DELETE /comments
</acceptance_criteria>
</task>

<task id="4">
<title>Fix DELETE /users/me/api-keys/{id}: 204 No Content</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 302-308)
- backend/internal/api/handlers/user_api_keys.go (line 269: w.WriteHeader(http.StatusNoContent))
</read_first>
<action>
Change DELETE /users/me/api-keys/{id} response from:
```
`{ "success": true }`
```
to:
```
`// 204 No Content`
```
</action>
<acceptance_criteria>
- grep "204 No Content" appears for DELETE /users/me/api-keys
</acceptance_criteria>
</task>

<task id="5">
<title>Fix DELETE /users/me/bookmarks/{id}: 204 No Content</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 360-367)
- backend/internal/api/handlers/bookmarks.go (line 129: w.WriteHeader(http.StatusNoContent))
</read_first>
<action>
Change DELETE /users/me/bookmarks/{id} response from:
```
`{ "success": true }`
```
to:
```
`// 204 No Content`
```
</action>
<acceptance_criteria>
- grep "204 No Content" appears for DELETE /users/me/bookmarks
</acceptance_criteria>
</task>

<task id="6">
<title>Fix POST /users/me/api-keys/{id}/regenerate: add response fields</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (lines 309-321)
- backend/internal/api/handlers/user_api_keys.go (lines 283-338 RegenerateAPIKeyResponse and handler)
</read_first>
<action>
Update regenerate response to match RegenerateAPIKeyResponse struct:
```json
{
  "data": {
    "id": "key_abc",
    "user_id": "user_abc",
    "name": "Production",
    "key": "solvr_sk_new...",
    "created_at": "2026-02-05T10:00:00Z",
    "updated_at": "2026-02-06T14:30:00Z"
  }
}
```
</action>
<acceptance_criteria>
- grep "user_id" appears in regenerate response
- grep "updated_at" appears in regenerate response
- grep "name" appears in regenerate response
</acceptance_criteria>
</task>

<task id="7">
<title>Add GET /me/auth-methods endpoint</title>
<read_first>
- backend/internal/api/handlers/me.go (lines 466-512 GetMyAuthMethods)
</read_first>
<action>
Add to User group:
```typescript
{
  method: "GET",
  path: "/me/auth-methods",
  description: "List authentication methods linked to current account",
  auth: "jwt",
  response: `{
  "auth_methods": [
    { "provider": "google", "linked_at": "2026-01-15T10:00:00Z", "last_used_at": "2026-03-19T08:00:00Z" },
    { "provider": "github", "linked_at": "2026-02-01T12:00:00Z", "last_used_at": "2026-03-18T15:00:00Z" }
  ]
}`,
}
```
</action>
<acceptance_criteria>
- grep "auth-methods" appears in the file
- grep "provider.*google" appears in the response
</acceptance_criteria>
</task>

<task id="8">
<title>Add DELETE /me endpoint</title>
<read_first>
- backend/internal/api/handlers/me.go (lines 514-593 DeleteMe)
</read_first>
<action>
Add to User group:
```typescript
{
  method: "DELETE",
  path: "/me",
  description: "Delete own account (soft delete). Unclaims all agents. Posts remain visible.",
  auth: "jwt",
  response: `{
  "data": {
    "message": "Account deleted successfully"
  }
}`,
}
```
</action>
<acceptance_criteria>
- grep "DELETE.*me" appears as a method+path combination
- grep "Account deleted" appears in the response
</acceptance_criteria>
</task>

<task id="9">
<title>Add Follows group (4 endpoints)</title>
<read_first>
- backend/internal/api/handlers/follows.go
- backend/internal/api/router.go (follow routes section)
</read_first>
<action>
Add a new "Follows" endpoint group with 4 endpoints. Read follows.go to get exact params and responses:
1. POST /users/{id}/follow — follow a user or agent (both auth)
2. DELETE /users/{id}/follow — unfollow (both auth)
3. GET /users/{id}/followers — list followers (no auth)
4. GET /users/{id}/following — list following (no auth)
</action>
<acceptance_criteria>
- grep "Follows" finds the new group
- grep "/follow" finds at least 2 endpoints
- grep "followers" finds the list followers endpoint
</acceptance_criteria>
</task>

<task id="10">
<title>Add Badges and Heartbeat endpoints</title>
<read_first>
- backend/internal/api/router.go (badges and heartbeat routes)
- backend/internal/api/handlers/agents.go (heartbeat handler if exists)
</read_first>
<action>
1. Add to User group or a new "Badges" group:
   - GET /users/{id}/badges — list user badges (no auth)
   - GET /agents/{id}/badges — list agent badges (no auth)
2. Add GET /heartbeat:
   - auth: "api_key"
   - description: "Agent liveness heartbeat — updates last_seen timestamp"
   - response: `{ "message": "ok", "last_seen": "2026-03-19T10:00:00Z" }`
</action>
<acceptance_criteria>
- grep "badges" finds badge endpoints
- grep "heartbeat" finds the heartbeat endpoint
</acceptance_criteria>
</task>

<task id="11">
<title>Add missing user management endpoints</title>
<read_first>
- backend/internal/api/handlers/me.go (GetMyDiff if exists)
- backend/internal/api/router.go (me/diff, me/storage, referral routes)
</read_first>
<action>
Add these endpoints if they exist in the router (read router.go to verify):
1. GET /me/diff — get user's recent changes/activity diff
2. GET /me/storage — get user's storage usage
3. GET /users/me/referral — get referral info
4. POST /auth/claim-referral — claim a referral code
These should be added to the User group. Read each handler to get exact params and responses.
</action>
<acceptance_criteria>
- Each endpoint that exists in router.go is documented in the data file
</acceptance_criteria>
</task>

<task id="12">
<title>Verify file stays under ~900 lines</title>
<read_first>
- frontend/components/api/api-endpoint-data-user.ts (after all edits)
</read_first>
<action>
Count lines. If over ~900, split follows/badges into a new file and import it in api-endpoints.tsx.
</action>
<acceptance_criteria>
- wc -l on the file shows <= 900 lines, OR a new split file is created and imported
</acceptance_criteria>
</task>
