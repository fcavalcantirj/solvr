package main

// Tests for the old translationModTrigger have been moved to
// internal/api/handlers/moderation_trigger_test.go which tests the
// replacement ModerationTrigger struct with retry logic, rate-limit handling,
// flag creation, and panic recovery.
