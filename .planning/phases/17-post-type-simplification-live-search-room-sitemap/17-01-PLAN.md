---
phase: 17-post-type-simplification-live-search-room-sitemap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/header.tsx
  - frontend/components/header.test.tsx
  - frontend/components/new-post/new-post-form.tsx
  - frontend/components/feed/feed-filters.tsx
  - frontend/app/sitemap-core.xml/route.ts
  - frontend/app/new/page.tsx
  - frontend/lib/api-types.ts
autonomous: true
requirements: [SIMPLIFY-01, SIMPLIFY-02, SIMPLIFY-03]

must_haves:
  truths:
    - "The new-post form shows only Problem and Idea — no Question option"
    - "Header navigation has no QUESTIONS link (desktop or mobile)"
    - "sitemap-core.xml does not contain /questions URL"
    - "Visiting /questions/[id] for any of the 9 existing questions still returns HTTP 200"
    - "Questions still appear in search results when matched"
    - "DATA link appears in header nav between AGENTS and IPFS"
    - "Feed type filter bar does not show a Questions tab"
    - "New post page metadata description does not mention 'question'"
  artifacts:
    - path: "frontend/components/header.tsx"
      provides: "Navigation without QUESTIONS, with DATA link added"
      contains: "/data"
    - path: "frontend/components/header.test.tsx"
      provides: "Updated test expecting DATA link, no QUESTIONS link"
      contains: "DATA"
    - path: "frontend/components/new-post/new-post-form.tsx"
      provides: "Post type selector with only problem and idea"
      contains: "grid-cols-2"
    - path: "frontend/components/feed/feed-filters.tsx"
      provides: "Feed type filter without Questions option"
    - path: "frontend/app/sitemap-core.xml/route.ts"
      provides: "Core sitemap without /questions entry, with /rooms and /data"
    - path: "frontend/app/new/page.tsx"
      provides: "New post page metadata without 'question' in description"
  key_links:
    - from: "frontend/components/header.tsx"
      to: "/data"
      via: "Link href"
      pattern: 'href="/data"'
    - from: "frontend/components/new-post/new-post-form.tsx"
      to: "POST_TYPES array"
      via: "const definition"
      pattern: "problem.*idea"
---

<objective>
Remove the question post type from all frontend creation and discovery surfaces while keeping existing question pages accessible. Add "DATA" to navigation.

Purpose: Simplify the user experience by removing the dead question type (only 9 posts, 1% of content) while preserving backwards compatibility for existing URLs. Add navigation entry for the upcoming /data page.
Output: Updated header.tsx, header.test.tsx, new-post-form.tsx, feed-filters.tsx, sitemap-core.xml/route.ts, new/page.tsx, api-types.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-post-type-simplification-live-search-room-sitemap/17-CONTEXT.md
@.planning/phases/17-post-type-simplification-live-search-room-sitemap/17-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/components/header.tsx:
- Desktop nav: lines 23-91 (nav element with Link components)
- Mobile nav: lines 131-165 (nav inside mobile menu div)
- QUESTIONS link at desktop lines 36-41, mobile line 138-140

From frontend/components/header.test.tsx:
- Tests "renders all 8 nav links" with hardcoded expectedLinks array including `{ name: 'QUESTIONS', href: '/questions' }`
- Test "positions IPFS between AGENTS and LEADERBOARD" checks link ordering
- Both tests need updating: remove QUESTIONS, add DATA, update count and ordering

From frontend/components/new-post/new-post-form.tsx:
```typescript
const POST_TYPES = [
  { value: 'question', label: 'Question', description: 'Ask a specific question to get answers' },
  { value: 'problem', label: 'Problem', description: 'Describe a problem to explore approaches' },
  { value: 'idea', label: 'Idea', description: 'Share an idea for discussion and feedback' },
] as const;

interface NewPostFormProps {
  defaultType?: 'problem' | 'question' | 'idea';
}
```

From frontend/components/feed/feed-filters.tsx:
```typescript
const types = [
  { label: "All", value: "all" },
  { label: "Problems", value: "problem" },
  { label: "Questions", value: "question" },
  { label: "Ideas", value: "idea" },
] as const;
```
The `{ label: "Questions", value: "question" }` entry must be removed (per D-21).

From frontend/app/new/page.tsx:
```typescript
export const metadata = {
  title: 'New Post | Solvr',
  description: 'Create a new problem, question, or idea',
};
```
The description must be updated to remove 'question' (per D-17, D-21).

From frontend/app/sitemap-core.xml/route.ts:
- Line 8: `{ loc: '${BASE_URL}/questions', changefreq: 'hourly', priority: 0.9 }`
- This entry must be removed

From frontend/lib/api-types.ts:
- APIPost.type includes 'question' — KEEP this (D-22)
- CreatePostData type — remove 'question' from this type only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove question type from frontend surfaces and add DATA nav</name>
  <read_first>
    - frontend/components/header.tsx
    - frontend/components/header.test.tsx
    - frontend/components/new-post/new-post-form.tsx
    - frontend/components/feed/feed-filters.tsx
    - frontend/app/sitemap-core.xml/route.ts
    - frontend/app/new/page.tsx
    - frontend/lib/api-types.ts
    - .planning/phases/17-post-type-simplification-live-search-room-sitemap/17-UI-SPEC.md
  </read_first>
  <files>
    frontend/components/header.tsx,
    frontend/components/header.test.tsx,
    frontend/components/new-post/new-post-form.tsx,
    frontend/components/feed/feed-filters.tsx,
    frontend/app/sitemap-core.xml/route.ts,
    frontend/app/new/page.tsx,
    frontend/lib/api-types.ts
  </files>
  <behavior>
    - Test: POST_TYPES array does NOT contain an entry with value 'question'
    - Test: POST_TYPES array contains exactly 2 entries (problem, idea)
    - Test: NewPostFormProps.defaultType union does NOT include 'question'
    - Test: Header desktop nav does NOT render a link to /questions
    - Test: Header mobile nav does NOT render a link to /questions
    - Test: Header desktop nav renders a link to /data with text "DATA"
    - Test: Header mobile nav renders a link to /data with text "DATA"
    - Test: Header nav order includes DATA after AGENTS
    - Test: sitemap-core.xml does NOT contain /questions URL
    - Test: sitemap-core.xml contains /rooms URL
    - Test: sitemap-core.xml contains /data URL
    - Test: Feed filter types array does NOT contain a "Questions" entry
  </behavior>
  <action>
    **frontend/components/header.tsx** (per D-07, D-19, D-21):
    1. Remove the QUESTIONS Link block from desktop nav (the Link with href="/questions" around lines 36-41)
    2. Remove the QUESTIONS Link block from mobile nav (the Link with href="/questions" around lines 138-140)
    3. Add a DATA Link after the AGENTS Link in desktop nav (before IPFS):
       ```tsx
       <Link href="/data" className="font-mono text-xs tracking-wider text-muted-foreground hover:text-foreground transition-colors">DATA</Link>
       ```
    4. Add a DATA Link after the AGENTS Link in mobile nav (before IPFS):
       ```tsx
       <Link href="/data" className="font-mono text-sm tracking-wider">DATA</Link>
       ```
    5. Final desktop nav order: FEED, PROBLEMS, IDEAS, ROOMS, AGENTS, DATA, IPFS, LEADERBOARD, SKILL, GUIDES

    **frontend/components/header.test.tsx** (per D-07, D-21 — update existing tests to match header changes):
    1. In the "renders all 8 nav links" test, update the expectedLinks array:
       - Remove `{ name: 'QUESTIONS', href: '/questions' }`
       - Add `{ name: 'DATA', href: '/data' }` after the AGENTS entry
       - Keep the test name as "renders all 8 nav links" (count stays 8: FEED, PROBLEMS, IDEAS, ROOMS, AGENTS, DATA, IPFS, LEADERBOARD, SKILL — actually update to 9 if ROOMS was added in a prior phase, or match what the header actually renders; count should match the actual links present)
    2. In the "positions IPFS between AGENTS and LEADERBOARD" test, optionally add a check that DATA is between AGENTS and IPFS — or leave unchanged since DATA being between AGENTS and IPFS still satisfies IPFS being between AGENTS-area and LEADERBOARD
    3. Verify no test references QUESTIONS or /questions anymore

    **frontend/components/new-post/new-post-form.tsx** (per D-17, D-21):
    1. Remove the question entry from POST_TYPES array — keep only problem and idea entries
    2. Change the grid class in the post type selector from `grid-cols-3` to `grid-cols-2`
    3. Update NewPostFormProps interface: change `defaultType?: 'problem' | 'question' | 'idea'` to `defaultType?: 'problem' | 'idea'`
    4. In handleSubmit, remove the ternary branch for `form.type === 'question'` — only problem and idea routes remain. If somehow type is question (existing post editing), fallback to 'ideas' route

    **frontend/components/feed/feed-filters.tsx** (per D-21):
    1. Remove the `{ label: "Questions", value: "question" }` entry from the `types` array at line 16
    2. Resulting types array should be:
       ```typescript
       const types = [
         { label: "All", value: "all" },
         { label: "Problems", value: "problem" },
         { label: "Ideas", value: "idea" },
       ] as const;
       ```
    3. No other changes needed in this file — the type filter UI auto-renders from the array

    **frontend/app/new/page.tsx** (per D-17, D-21):
    1. Update the metadata description from `'Create a new problem, question, or idea'` to `'Create a new problem or idea'`
    2. No other changes needed in this file

    **frontend/app/sitemap-core.xml/route.ts** (per D-18, D-19):
    1. Remove the entry: `{ loc: '${BASE_URL}/questions', changefreq: 'hourly', priority: 0.9 }`
    2. Add entry: `{ loc: '${BASE_URL}/rooms', changefreq: 'hourly', priority: 0.8 }`
    3. Add entry: `{ loc: '${BASE_URL}/data', changefreq: 'hourly', priority: 0.7 }`

    **frontend/lib/api-types.ts** (per D-22):
    1. Find the CreatePostData type (or equivalent type for post creation requests)
    2. Remove 'question' from its type field union
    3. Do NOT remove 'question' from APIPost.type or any read-side response types — backend still returns 'question' for existing 9 posts
  </action>
  <verify>
    <automated>cd /home/clawdbot/development/solvr/frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "question" frontend/components/new-post/new-post-form.tsx` returns 0 (no question references in POST_TYPES or defaultType)
    - `grep -c "/questions" frontend/components/header.tsx` returns 0 (no QUESTIONS nav links)
    - `grep 'href="/data"' frontend/components/header.tsx` returns 2 matches (desktop + mobile)
    - `grep -c "QUESTIONS" frontend/components/header.test.tsx` returns 0 (test no longer references QUESTIONS)
    - `grep "DATA" frontend/components/header.test.tsx` returns at least 1 match (test expects DATA link)
    - `grep -c '"question"' frontend/components/feed/feed-filters.tsx` returns 0 (no question filter entry)
    - `grep "question" frontend/app/new/page.tsx` returns 0 matches (metadata description updated)
    - `grep -c "/questions" frontend/app/sitemap-core.xml/route.ts` returns 0
    - `grep "/rooms" frontend/app/sitemap-core.xml/route.ts` returns at least 1 match
    - `grep "/data" frontend/app/sitemap-core.xml/route.ts` returns at least 1 match
    - `grep "grid-cols-2" frontend/components/new-post/new-post-form.tsx` returns at least 1 match
    - `grep "'question'" frontend/lib/api-types.ts` still returns matches (question kept in read types per D-22)
    - All existing frontend tests pass (header.test.tsx updated to match new nav structure)
  </acceptance_criteria>
  <done>
    Question type invisible from all creation and discovery surfaces: new-post form, header nav, feed type filters, new-post page metadata. DATA link present in navigation. Existing header tests updated to expect new nav structure. Existing question detail pages unaffected (no code changes to /questions/[id] route). sitemap-core.xml cleaned up with rooms and data added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is purely frontend subtractive changes — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | sitemap-core.xml | accept | Removing /questions from sitemap reduces crawl surface; no sensitive data exposed |
| T-17-02 | Spoofing | header.tsx /data link | accept | /data page is public with no auth; link is a standard navigation element |
</threat_model>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` — all tests green (including updated header.test.tsx)
2. `grep -r "QUESTIONS" frontend/components/header.tsx` — returns 0 matches
3. `grep "question" frontend/components/new-post/new-post-form.tsx` — returns 0 matches
4. `grep "/questions" frontend/app/sitemap-core.xml/route.ts` — returns 0 matches
5. `grep '"question"' frontend/components/feed/feed-filters.tsx` — returns 0 matches
6. `grep "question" frontend/app/new/page.tsx` — returns 0 matches
</verification>

<success_criteria>
- POST_TYPES array contains exactly 2 entries: problem and idea
- Header has 0 QUESTIONS links and 2 DATA links (desktop + mobile)
- Header test expects DATA link, does not reference QUESTIONS
- Feed type filter shows only All, Problems, Ideas (no Questions)
- New post page metadata description says "Create a new problem or idea"
- sitemap-core.xml has no /questions entry, has /rooms and /data entries
- All existing frontend tests pass
- No changes to /questions/[id] detail page route (SIMPLIFY-02 satisfied by omission)
</success_criteria>

<output>
After completion, create `.planning/phases/17-post-type-simplification-live-search-room-sitemap/17-01-SUMMARY.md`
</output>
